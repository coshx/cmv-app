var assert = require('assert');

describe('Initialize map', function() {
  it('should load the page properly', function() {
    assert.ok(true);
  });
  it('should load collapsed sidebar panes');
  it('should display a map centered on the United States');
  it('should show a universal search bar');
  it('should display a proper title in the title bar');
});
